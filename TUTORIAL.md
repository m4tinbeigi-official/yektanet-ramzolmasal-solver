# 📚 آموزش جامع و مرجع گام‌به‌گام حل چالش‌های مسابقه رمزالمثل یکتانت (Walkthrough & Tutorial)

<p align="center">
  <a href="https://devsponsors.github.io"><img src="https://img.shields.io/badge/DevSponsors-Verified_OSS-6366f1?style=for-the-badge&logo=github" alt="DevSponsors Verified"></a>
  <a href="https://devsponsors.github.io"><img src="https://img.shields.io/badge/Sponsor-DevSponsors_Hub-emerald?style=for-the-badge&logo=github-sponsors" alt="DevSponsors Sponsor"></a>
  <a href="https://devsponsors.github.io/mediakit.html"><img src="https://img.shields.io/badge/Infrastructure-DevSponsors_Cloud-ec4899?style=for-the-badge&logo=server" alt="DevSponsors Cloud"></a>
</p>

این سند، راهنمای کامل، موشکافانه و تحلیلی چگونگی کشف، مهندسی معکوس، بهره‌برداری از آسیب‌پذیری‌ها و حل تمامی ۱۰ مرحله مسابقه آنلاین «رمزالمثل» یکتانت است.

---

## 🤝 همکاران و تشکر ویژه (Special Credits)

* 🌟 **اردلان کتابچی ([@ketabchi-ar](https://github.com/ketabchi-ar) / [@Ketabchi0](https://t.me/Ketabchi0)):**  
  تشکر و قدردانی ویژه از **اردلان کتابچی** برای همکاری کلیدی، مهندسی معکوس و کشف راه‌حل مراحل مسابقه:
  * 🥒 **مرحله خیار (`khiar`):** کشف و تحلیل منطق ضرب‌المثل *«هرچه بگندد نمکش می‌زنند»* و شناسایی سالت هش (شناسه کاربری یکتا) در ترکیب با شماره موبایل ($\text{SHA-256}(\text{phone} + \text{id})$).
  * 🌀 **مرحله سرگیجه (`sargijeh`):** همراهی و تحلیل چرخش‌های ماتریس SVG و رسیدن به پرچم نهایی.
  * ⚗️ **مرحله کیمیاگر (`kimiagar`):** تحلیل جریان وب‌سوکت و غربالگری کلمات طلایی.
  * 💻 **مخزن رایت‌آپ اردلان در گیت‌هاب:** [ketabchi-ar/yektanet-ramzolmasal-ctf-writeup](https://github.com/ketabchi-ar/yektanet-ramzolmasal-ctf-writeup)

---

## 🎯 مقدمه و ساختار مسابقه

مسابقه «رمزالمثل» یک چالش امنیتی/معمایی (CTF) است که در آن هر مرحله بر پایه یک یا چند **ضرب‌المثل و اصطلاح اصیل فارسی**، همراه با **مفاهیم علوم کامپیوتر، امنیت وب، رمزنگاری، مهندسی معکوس و پردازش سیگنال** طراحی شده است.

---

# 📖 فهرست تفصیلی آموزش حل تمامی مراحل

---

### 🧩 ۱. مرحله اول: تاتی تاتی (`tati-tati`)

* **🎯 صورت مسئله:**  
  *«پاسخ این یکی را از همان اول به تو گفتیم: `YEK{YektaNet}` — دقیقا همین، حرف به حرف. تمام سؤال، پیدا کردن جایی بود که باید ثبتش کنی. اگر داری این متن را می‌خوانی، یعنی پیدایش کرده‌ای.»*
* **💡 منطق و سرنخ:**  
  «تاتی تاتی» به اولین گام‌ها برای شروع راه رفتن اشاره دارد. در وب‌اپلیکیشن مسابقه هیچ دکمه یا فرم ظاهری برای ثبت وجود ندارد؛ بنابراین اولین گام باز کردن ابزارهای توسعه‌دهنده مرورگر (**DevTools**) است.
* **🛠️ آموزش گام‌به‌گام حل:**
  1. در صفحه مسابقه، کلید `F12` (یا `Ctrl+Shift+I` / `Cmd+Option+I`) را بزنید و به تب **Console** بروید.
  2. فایل `ctf.js` لود شده در صفحه، توابع تعاملی را در فضای سراسری `window.ctf` تعریف کرده است:
     * `puzzles()`: مشاهده فهرست معماها و وضعیت باز بودن آن‌ها.
     * `select(id)`: انتخاب یک چالش برای ارسال پاسخ.
     * `submit(flag)`: ارسال پاسخ برای چالش انتخاب‌شده.
     * `me()`: مشاهده شناسه کاربری و وضعیت مراحل حل‌شده.
     * `board()`: مشاهده جدول امتیازات و تعداد حل‌های هر مرحله.
  3. دستورات زیر را در کنسول اجرا کنید:
     ```javascript
     select("tati-tati");
     submit("YEK{YektaNet}");
     ```
* **🚩 پرچم (Flag):**  
  `YEK{YektaNet}`

---

### 🧩 ۲. مرحله دوم: کلاغ (`kalagh`)

* **🎯 صورت مسئله:**  
  *«یه کلاغه یه خبری آورده، ولی می‌دونی که حرف تو دهن کلاغ‌جماعت بچرخه، دیگه اون حرف اولی نمی‌مونه. دنبال قصه رو بگیر، ببین آخرش چی به گوشت می‌رسه.»*
* **💡 منطق ضرب‌المثل:**  
  ضرب‌المثل مشهور فارسی: **«یک کلاغ، چهل کلاغ»**. این نشان می‌دهد که پیام خبررسانی باید دقیقاً **۴۰ بار** دهان‌به‌دهان بچرخد و پیام مرحله چهلم، پرچم واقعی است.
* **🔬 مکانیسم فنی و الگوریتم Proof-of-Work:**  
  سرور در اندپوینت `/api/kalagh` یک مکانیسم اثبات کار (Proof-of-Work) قرار داده است:
  * در هر گام یک رشته `payam` (هش ۶۴ کاراکتری) و یک شماره گام (`shomare`) داده می‌شود.
  * کلاینت باید یک عدد صحیح نامنفی به نام `nonce` بیابد به طوری که هش **FNV-1a** (نسخه ۳۲ بیتی) رشته `payam:nonce` مقداری کمتر از **۲۰۴۸** داشته باشد (اصطلاحاً خوش‌صدا باشد).
  * فرمول FNV-1a در ۳۲ بیت:
    $$h_0 = 2166136261$$
    $$h_{i} = (h_{i-1} \oplus \text{byte}_i) \times 16777619 \pmod{2^{32}}$$
  * شرط خوش‌صدایی: $h < 2048$.
* **🛠️ آموزش گام‌به‌گام حل:**
  1. کد سالور پرسرعت C را از مسیر `src/c_kalagh.c` کامپایل کنید:
     ```bash
     clang -O3 src/c_kalagh.c -lcurl -o kalagh_solver
     ```
  2. اسکریپت را با کوکی‌های لاگین اجرا کنید:
     ```bash
     ./kalagh_solver cookies.txt
     ```
  3. برنامه در کمتر از ۲ ثانیه، ۴۰ مرحله را محاسبه و پیام گام چهلم را استخراج می‌کند.
  4. ثبت پرچم:
     ```javascript
     select("kalagh");
     submit("YEK{<PAYAM_STEP_40>}");
     ```
* **🚩 پرچم نمونه:**  
  `YEK{56d8e1ff5ed4bdfdb04cc1f7b43185987069ca74497c00c4551836d3469f9a2c}`

---

### 🧩 ۳. مرحله سوم: نگهبان دربار (`negahban`)

* **🎯 صورت مسئله:**  
  *«نگهبان دربار، سند تو را نمی‌پذیرد مگر آنکه دو متن کاملا متفاوت بیاوری که نگهبان حواس‌پرت، مهر هر دو را یکی ببیند. نگهبان دربار به لطف یک خطای قدیمی در سنجیدن، گول ظاهر این شاهدان ساختگی را می‌خورد. دو سند فریبنده بساز، مهر تایید را بگیر و کلید آزادی را پیدا کن.»*
* **💡 منطق و آسیب‌پذیری:**  
  خطای قدیمی در سنجیدن به باگ معروف **Type Juggling در زبان PHP** (مقایسه سست با `==`) اشاره دارد. در PHP اگر دو رشته با قالب `0e...` هش شوند و مابقی آن‌ها فقط رقم باشد (`0e\d+`)، مفسر PHP هر دو را به صورت نماد علمی عدد صفر (`0 × 10^x = 0`) ارزیابی کرده و برابر می‌داند (`"0e123..." == "0e456..." -> True`).
* **🔬 مقادیر جادویی (Magic Hashes):**
  * رشته اول: `240610708` -> هش: `0e462097431906509019562988736854`
  * رشته دوم: `QNKCDZO` -> هش: `0e830400451993494058024219903391`
* **🛠️ آموزش گام‌به‌گام حل:**
  1. یک درخواست POST به اندپوینت `/api/negahban` با دو پارامتر `sanad1` و `sanad2` ارسال کنید:
     ```bash
     curl -s -X POST "https://256.yektanet.tech/ramzolmasal/api/negahban" \
       -b cookies.txt \
       -d "sanad1=240610708&sanad2=QNKCDZO"
     ```
  2. در پاسخ سرور، کلید آزادی درون تگ `<code>` بازگردانده می‌شود:
     ```html
     <p>سندها پذیرفته شد. کلید آزادی شما: <code>b2cc0153bbafead0</code></p>
     ```
  3. کلید را در قالب پرچم ثبت کنید:
     ```javascript
     select("negahban");
     submit("YEK{b2cc0153bbafead0}");
     ```

---

### 🧩 ۴. مرحله چهارم: شاهد روباه (`shahed`)

* **🎯 صورت مسئله:**  
  *«تو شاهد میخوای چیکار؟ خودت شهادت بده واسه خودت! شاهدی که خودش را شاهد خودش می‌گیرد، از کنار دربان خواب‌آلود بی هیچ پرسشی رد می‌شود.»*
* **💡 منطق ضرب‌المثل:**  
  ضرب‌المثل کهن فارسی: **«به روباه گفتند شاهدت کیه؟ گفت دمم!»**.
* **🔬 مکانیسم فنی:**  
  سرور فرآیند احراز اصالت را به تصدیق سلف‌سایند (Self-Attestation / Self-Signed) توسط مرورگر متصل کرده است. با ترکیب شناسه کاربری و نشست جاری، کلاینت خودش به خودش شهادت می‌دهد و کلید معتبر را در نشست فعال می‌سازد.

---

### 🧩 ۵. مرحله پنجم: ارزش نبات (`nabat`)

* **🎯 صورت مسئله:**  
  *«تو این بازار یه قناد هست که قیمت جنسش رو به هر مشتری‌ای نمی‌گه. کی رو راه می‌دن و کی رو نه، سر در بازار نوشته؛ همون نوشته‌ای که همه از زیرش رد می‌شن و کسی نمی‌خونه.»*
* **💡 منطق ضرب‌المثل و معماری:**  
  * سر در بازار که همه از زیرش رد می‌شوند و نمی‌خوانند: **فایل `robots.txt`**.
  * ضرب‌المثل فارسی: **«خر چه داند قیمت نقل و نبات!»**.
* **🛠️ آموزش گام‌به‌گام حل:**
  1. بررسی فایل `https://256.yektanet.tech/ramzolmasal/robots.txt`:
     ```text
     User-agent: khar
     Allow: /ramzolmasal/api/price
     
     User-agent: *
     Disallow: /
     ```
  2. ارسال متد `OPTIONS` به اندپوینت `/api/price` برای کشف پارامترها:
     ```bash
     curl -s -X OPTIONS "https://256.yektanet.tech/ramzolmasal/api/price" \
       -H "User-Agent: khar" -b cookies.txt
     ```
     پاسخ نشان می‌دهد پارامتر `product_name` با نام فارسی جنس الزامی است.
  3. ارسال درخواست با هدر `User-Agent: khar` و پارامتر `product_name=نبات`:
     ```bash
     curl -s "https://256.yektanet.tech/ramzolmasal/api/price?product_name=نبات" \
       -H "User-Agent: khar" -b cookies.txt
     ```
  4. پاسخ سرور حاوی قیمت ریالی نبات است:
     ```json
     {
       "payam": "نوش جان. این قیمت مال توئه...",
       "price": "۵۴۲۵۸۲۵۹۶۷۱۲۳۶۹۷",
       "product_name": "نبات",
       "unit": "ریال"
     }
     ```
  5. ثبت پرچم:
     ```javascript
     select("nabat");
     submit("YEK{۵۴۲۵۸۲۵۹۶۷۱۲۳۶۹۷}");
     ```

---

### 🧩 ۶. مرحله ششم: سرگیجه (`sargijeh`)

* **🎯 صورت مسئله:**  
  *«از هر طرف که رفتم جز وحشتم نیافزود، تا یه ربع چرخیدیم»*
* **💡 منطق و تحلیل (با همراهی اردلان کتابچی):**  
  * اشاره به بیت مشهور حافظ شیرازی:  
    *«از هر طرف که رفتم جز وحشتم نیافزود / زنهار از این بیابان وین راه بی‌نهایت»*
  * عبارت «تا یه ربع چرخیدیم» یعنی چرخش ۹۰ درجه (یک ربع ساعت = ۹۰ درجه زاویه).
* **🔬 مکانیسم فنی:**  
  مسئله یک ماتریس برداری ۱۲ در ۲۳ در فایل SVG است که مسیرها، گره‌ها و موانع انشعاب را مشخص می‌کند. با پیمایش گراف از نقطه شروع و اعمال قوانین چرخش ۹۰ درجه در محل تقاطع‌ها، مسیر منتهی به خروجی استخراج و حل می‌شود.
* **🛠️ ابزارهای حل:**  
  اسکریپت‌های `src/analyze_sargijeh.py` و `src/sim_sargijeh.py`.

---

### 🧩 ۷. مرحله هفتم: خیار (`khiar`)

* **🎯 صورت مسئله:**  
  *«شمارت داره می‌گنده. به ضرب‌المثل‌های فارسی فکر کن.»*
* **💡 منطق ضرب‌المثل و رمزنگاری (با تحلیل و کشف اردلان کتابچی):**  
  * ضرب‌المثل اصیل فارسی: **«هرچه بگندد نمکش می‌زنند، وای به روزی که بگندد نمک!»**.
  * در امنیت و علوم کامپیوتر، برای جلوگیری از لو رفتن یا فساد پسوردها و شناسه‌ها، به آن‌ها **Salt (نمک)** اضافه می‌کنند.
* **🔬 مکانیسم فنی:**  
  * در این چالش، شناسه یکتای کاربری (`id` در خروجی `api/me`) به عنوان **نمک (Salt)** عمل می‌کند.
  * با ترکیب شماره موبایل کاربر (`phone`) و شناسه کاربری (`id`) و محاسبه هش **SHA-256** بر روی آن، پرچم یکتای مرحله خیار تولید می‌شود:
    $$\text{Flag} = \text{YEK}\{\text{SHA-256}(\text{phone} + \text{id})\}$$
* **🛠️ اسکریپت خودکار حل خیار:** `src/solve_khiar.py`
  ```python
  import hashlib

  phone = "09127047813"
  user_id = "88004c9a"
  flag_hash = hashlib.sha256(f"{phone}{user_id}".encode()).hexdigest()
  print(f"YEK{{{flag_hash}}}")
  ```

---

### 🧩 ۸. مرحله هشتم: یاایکس (`ya-x`)

* **🎯 صورت مسئله:**  
  *«یه چیز بهت داده‌ایم که به تنهایی به کار نمیاد و صدایی ازش در نمیاد. صداشو در بیار.»*
* **💡 منطق ضرب‌المثل:**  
  ضرب‌المثل معروف: **«دستِ تنها صدا نداره!»**.
* **🔬 مکانیسم فنی:**  
  فایل ارائه‌شده `chap.bin` حاوی ۳۰۷۲ بایت داده‌های باینری کانال چپ (Left Channel) یک صدای استریو است. دست تنها (کانال چپ به تنهایی) صدایی نمی‌دهد؛ باید با کانال راست یا عملیات دوقلو (Stereo Combining / XOR) ترکیب شود تا صدای دست زدن (Clap) و فرکانس‌های حاوی کاراکترهای پرچم آشکار شوند.
* **🛠️ ابزارهای حل:**  
  اسکریپت‌های `src/analyze_chap_yax.py` و `src/solve_yax.py`.

---

### 🧩 ۹. مرحله نهم: کیمیاگر (`kimiagar`)

* **🎯 صورت مسئله:**  
  *«زرگر که باشی، غربال دست میگیری. کارگاه این‌جاست: `api/kimiagar`»*
* **🔬 مکانیسم فنی:**  
  با ارسال درخواست HTTP به `/api/kimiagar`، سرور کد خطای **HTTP 426 (Upgrade Required)** بازمی‌گرداند با این متن:
  *«برای استخراج از این معدن بی‌پایان، جای رفت‌وبرگشت‌های مقطعی، باید یک تونل ارتباطی همیشه‌باز و دوطرفه تا اعماقش حفر کنی...»*
  این یعنی ارتباط باید از طریق پروتکل **WebSocket** برقرار شود:
  `wss://256.yektanet.tech/ramzolmasal/api/kimiagar`
* **🛠️ ابزارهای حل (با همراهی اردلان کتابچی):**  
  سرور ۶۴ توکن منحصربه‌فرد بر بستر وب‌سوکت ارسال می‌کند. توکن‌های ابتدایی نقش جدول الفبا (Base64 Mapping) را دارند و بایت‌های بعدی با اسکریپت `src/solve_kimiagar_ws.py` و `src/kimiagar_decoder.py` رمزگشایی می‌شوند.

---

### 🧩 ۱۰. مرحله دهم: دیوار به دیوار (`pelak`)

* **🎯 صورت مسئله:**  
  *«اینجا خونته. هرچی تو خونه‌ی خودت هست، به چشمت نمیاد.»*
* **💡 منطق ضرب‌المثل:**  
  اشاره به ضرب‌المثل‌های همسایگی و پلاک مجاور (*«چراغی که به خانه رواست...»* یا *«اول همسایه را ببین، بعد خانه را بخر»*).
* **🔬 مکانیسم فنی:**  
  سرنخ سرور: *«همیشه شماره تلفنشو می‌چسبونه به شماره موبایلش و میشه رمزش.»* — استخراج شماره تلفن ثابت/منزل و ترکیب آن با شماره همراه جهت ثبت پرچم نهایی در فرمت `YEK{<HOME_PHONE><MOBILE_PHONE>}`.

---

## 🚀 نحوه اجرای سالورها

تمام کدهای سالور در پوشه `src/` قرار دارند:

```bash
# ۱. سالور کلاغ (C پرسرعت)
clang -O3 src/c_kalagh.c -lcurl -o kalagh_solver && ./kalagh_solver cookies.txt

# ۲. سالور نگهبان دربار (Type Juggling)
python3 src/solve_negahban.py

# ۳. سالور ارزش نبات (Robots.txt & User-Agent)
python3 src/solve_nabat.py

# ۴. سالور خیار (Salt Hashing)
python3 src/solve_khiar.py

# ۵. سالور کیمیاگر (WebSocket Stream)
python3 src/solve_kimiagar_ws.py

# ۶. تحلیلگر فایل صوتی یاایکس
python3 src/analyze_chap_yax.py
```
